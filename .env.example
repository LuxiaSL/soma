# =============================================================================
# Soma Configuration
# =============================================================================
# Copy this file to .env and fill in the values
# =============================================================================

# -----------------------------------------------------------------------------
# API Server
# -----------------------------------------------------------------------------

# Port for the Soma API server (default: 3100)
SOMA_PORT=3100

# Path to SQLite database file (default: ./data/soma.db)
# The directory will be created if it doesn't exist
SOMA_DATABASE_PATH=./data/soma.db

# Service tokens for API authentication (comma-separated)
# These tokens are used by ChapterX bots and other services to authenticate
# Generate secure random tokens (e.g., `openssl rand -hex 32`)
SOMA_SERVICE_TOKENS=your-secure-token-here,another-token-for-different-service

# -----------------------------------------------------------------------------
# Discord Bot (Optional)
# -----------------------------------------------------------------------------
# If not set, Soma runs in API-only mode (no Discord commands/reactions)

# Discord bot token - get this from Discord Developer Portal
# See DISCORD_BOT_SETUP.md for detailed instructions
SOMA_DISCORD_TOKEN=

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------

# Log level: debug, info, warn, error (default: info in production, debug in dev)
LOG_LEVEL=info

# Node environment: development, production
NODE_ENV=development

